import { StyleSheet } from 'react-native';

const inputStyles = StyleSheet.create({
    input: {
      height: 40,
      borderColor: '#F7FAFD',
      borderWidth: 1,
      borderRadius: 10,
      marginBottom: 20,
    },
  })
  
export default inputStyles;